org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Pin the Gradle daemon to JDK 17 regardless of which JDK launches it
# (e.g. Android Studio's bundled JBR, currently Java 25, which Gradle 8.12
# does not support). AGP 8.7.3 + Kotlin 2.1.0 are verified to work on 17.
org.gradle.java.home=C:/Program Files/Java/jdk-17
